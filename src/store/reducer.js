const defaultState = {
    inputValue:'Write Someting',
    list:[
        '早上八点开晨会，分配今天的代码任务',
        '早九点和项目经理沟通会',
        '早上八点开晨会，分配今天的代码任务'
    ]
}

export default (state = defaultState,action)=>{
    return state
}